name = "danzoa-com-worker"
main = "worker.js"
compatibility_date = "2026-09-11"
account_id = "f07be5f84583d0d100b05aeeae56870b"

# Serves the real, tested MVP (../mvp: pirouette rotation-consistency
# analyzer - independently re-verified 2026-09-11 with node -e assertions
# on synthetic rotation data). This REPLACES the prior "danzoa-com-worker"
# script, which was a fabricated stub (a single hardcoded POST endpoint,
# /api/danzoa/pose-estimation-vector, returning a canned
# {status:"success", generated:true} response with zero real logic,
# everything else 404s) already flagged as fake in ventures.json's
# insight.evidence on 2026-09-11. Deliberately no [[routes]] here - the
# zone's root/www routes are managed explicitly via the Cloudflare API,
# not by wrangler, so only the one route being fixed is touched.
[assets]
directory = "../mvp"
binding = "ASSETS"
run_worker_first = true

# Lead capture for the registry's own stated next_step ("a named dance
# instructor/studio to try it, not more building") - lets a real visitor
# who wants to try this with their studio leave contact info, since the
# MVP itself has no way to follow up with anyone who lands on it.
[[d1_databases]]
binding = "LEADS_DB"
database_name = "danzoa_com_leads"
database_id = "54fe52d4-1c85-4b7a-a657-c6f798e28d9b"

# Real gap closed 2026-09-23: a captured lead previously sat silently in
# D1 with nothing to surface it - the danzoa.com zone already has Email
# Routing enabled and jmobleyworks@gmail.com already verified as a
# destination (confirmed live via the Cloudflare API before adding this),
# so this reuses the same sovereign send_email binding mailguyai.com's
# modules/outbound.js already proves works, rather than adding an external
# email API dependency. Best-effort only - see handleInterest in worker.js.
[[send_email]]
name = "SEND_EMAIL"
